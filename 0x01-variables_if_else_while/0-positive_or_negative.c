#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/**
 *main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	/* check the condition */
	if (n > 0)
	{
		printf("d n is positive\n";
	}
	else if (n == 0)
	{
		printf("%d n is zero\n");
	}
	else
	{
		printf("%d n is negative\n");
	}
	return (0);
}
